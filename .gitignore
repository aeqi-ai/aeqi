**/target/
*.swp
*.swo
.DS_Store

# Runtime state
.aeqi/
**/.tasks/
*.db
*.db-wal
*.db-shm

# Build artifacts
apps/ui/node_modules/
apps/ui/dist/
apps/ui/*.tsbuildinfo
apps/landing/node_modules/
apps/landing/dist/
node_modules/

# Local config
apps/ui/.env
apps/ui/.env*.local
.env
.env.local

# Agent runtime state (track templates, ignore state)
agents/*/.aeqi/
!agents/*/agent.md

# Project directories (personal)
projects/*/
!projects/shared/
!projects/README.md

# Claude Code
.claude/worktrees/
apps/ui/dist-storybook/
apps/ui/apps/

# Night-shift / research scratch (local coordination state, not product code)
tmp/
NIGHT_SHIFT_BRIEF.md
aeqi_architecture_idea.md
scripts/tg-nightshift.sh

# Internal coordination artifacts — never commit
notes/
LUCAS_DREAM.md
OVERNIGHT_*.md
OVERNIGHT-*.md
AGENTS.md
QUALITY_GATES.md
CODEX_HANDOFF.md
PLAN.md
PROMPT.md
TODO.md
VISION.md
security_tests.rs

# Operator-local config (use `config/aeqi.example.toml` as template)
config/aeqi.toml
config/platform.toml
config/ports.toml

# Personal deploy scripts
scripts/deploy.sh
scripts/check-recall.sh.real

# Internal dogfood / UX walks / observation reports — never committed
# (UX screenshots, walk reports, internal briefs, dogfood notes)
.observations/

# Internal AI-pair-programming context — full of cost notes, prod paths,
# session-specific recipes. Self-hosters and contributors don't need this;
# generic project conventions live in CONTRIBUTING.md / docs/.
CLAUDE.md
apps/ui/CLAUDE.md
.private/

# Internal UX walk + audit tooling (hits prod URLs, references our
# dogfood flow, mints JWTs against our auth). Self-hosters don't need these.
scripts/ux-*.mjs
scripts/ux-*.sh
scripts/persona-walk-*.mjs
scripts/AUDIT.md
scripts/_*.mjs
scripts/tg-*.sh
scripts/bridge-wizard-verify.mjs
scripts/dogfood-*.mjs
